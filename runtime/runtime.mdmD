import std

(), nip, ()
/// Initialisiert die Runtime und laedt die Typdefinitionen
(), initializeRuntime, (_)

/// Bereitet den Speicher für Input und Output eines dState vor
(inputType), prepareMemoryForDState, (_)

/// Erkennung und Ausführung von Processing-Tokens
(inputData, processingToken), executeProcessing, (_)

/// die dSeq geht nur dann in einem neuen dState über, sollte sich bei dem current dState etwas im content geändert haben!